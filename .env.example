# Generate with: chainlit create-secret
CHAINLIT_AUTH_SECRET=

# Login credentials for the local app (pick your own)
APP_USERNAME=adam
APP_PASSWORD=change-me

# AWS configuration (uses your default CLI profile)
AWS_PROFILE=default
AWS_DEFAULT_REGION=us-east-1
